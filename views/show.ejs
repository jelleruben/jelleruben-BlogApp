<%- include("partials/header") %>
<div class="ui main text container segment">
    <div class="ui huge header"><%= blog.title %></div>
    <div class="ui huge header"><%= blog.catogory %></div>
        <div class="item">
            <img class="ui centered rounded image" src="http://<%= blog.image %>">
            <div class="content">
                <span><%= blog.created.toDateString() %></span>
            </div>
          
            
            <p>
            <div class="description">
                <p><%-  blog.body %></p><br>
            </div>
            <a class="ui blue basic button" href="/blogs/"><i class="left floated vertical reply icon"></i></a>
            <a class="ui blue basic button" href="http://<%= blog.website %>" target="_blank"><i class="left floated globe icon"></i></a>
            <form id="delete" action="/blogs/<%= blog._id %>?_method=DELETE" method="POST">
            <button class="ui red basic button right floated" ><i class="right floated trash alternate icon"></i></button>
            <a class="ui green basic button right floated" href="/blogs/<%= blog._id %>/edit"><i class="right floated pencil alternate icon"></i></i></a>
            </form></p>
        </div>
    </div>
</div>
<%- include("partials/footer") %>